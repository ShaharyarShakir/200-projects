{
  "dependencies": {},
  "scripts": {
    "dev": "concurrently \"bun run server\" \"bun run client\"",
    "server": "bun --cwd server dev",
    "client": "bun --cwd client dev"
  },
  "devDependencies": {
    "concurrently": "^10.0.4",
    "nodemon": "^3.1.14"
  }
}